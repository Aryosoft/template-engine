<%# $layout: {pageTitle: string?, body: string} %>
<% const getTitle=()=> {
        let pageTitle = 'Aryosoft';
        pageTitle = ($model.pageTitle ?? '').toString().trim();
        if($model.pageTitle != '')
            pageTitle += ` | ${$model.pageTitle}`;
        return pageTitle;
    }

    %>
    <!DOCTYPE html>
    <html>

    <head>
        <title>
            <%=getTitle()%>
        </title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
            integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
            crossorigin="anonymous">
        <style>
            header {
                border-bottom: solid 1px #ccc;
                margin-bottom: 20px;
            }

            footer {
                border-top: solid 1px #ccc;
                margin-top: 20px;
            }
        </style>
    </head>

    <body>
        <header>
            <%- this.include('header', $model) %>
        </header>
        <div class="container">
            <%- this.include($model.content, $model) %>
        </div>
        <footer>
            <%- this.include('footer', $model) %>
        </footer>
    </body>

    </html>